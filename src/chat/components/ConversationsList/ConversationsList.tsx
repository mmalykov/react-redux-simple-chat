import React from "react";
import {Grid} from "@material-ui/core";

export const ConversationsList: React.FC = () => {
    return (
        <Grid item xs={4}>
            Conversations list
        </Grid>
    );
};
